# 头像引擎系统 · 项目大纲

> 版本：v1.0 初稿  
> 日期：2026-09-07  
> 说明：本大纲为整个项目的顶层设计文件，后续所有功能设计、数据库设计、API 设计、前端实现均严格依据本大纲执行。如有调整，必须先修改本大纲再开发。

---

## 一、项目概述

### 1.1 项目定位
一套面向开发者与普通用户的 **AI 头像生成引擎系统**。用户上传真人头像，通过豆包（Doubao Seedream）图生图能力生成卡通头像。系统对外提供 API，对内集成完整的管理后台、用户体系、积分体系、订单体系与头像池体系。

### 1.2 核心价值
- 对普通用户：一键生成个性化卡通头像，支持颜色、形状、风格等多维度定制。
- 对开发者：通过 KEY 调用 API，批量生成并管理子用户头像。
- 对平台方：所有生成的头像自动汇入公共头像池，越用越丰富，形成内容沉淀。

### 1.3 豆包图生图 API（核心引擎）
```
POST https://ark.cn-beijing.volces.com/api/v3/images/generations
Header: Authorization: Bearer {API_KEY}
Body:
{
  "model": "doubao-seedream-5-0-260128",
  "prompt": "卡通化提示词",
  "image": "上传图片的公网URL",
  "sequential_image_generation": "disabled",
  "response_format": "url",
  "size": "2K",
  "stream": false,
  "watermark": true
}
```
> 系统需先将用户上传的图片上传到可公网访问的存储（如对象存储/CDN），再将 URL 传给豆包接口。

---

## 二、技术栈选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端语言 | PHP 8.1+ | 贴合用户技术栈，宝塔面板部署（最低 8.1，推荐 8.2/8.3） |
| 后端框架 | 原生 PHP + 自写路由/MVC | 零第三方框架依赖，自实现路由分发、配置加载、轻量 ORM（PDO 封装）、视图层、任务队列 |
| 数据库 | MySQL 5.6 | 主数据库，兼容性广、资源占用低（注意：JSON 字段改用 TEXT 存结构化数据，代码层做 encode/decode） |
| 缓存 | Redis 5.0+ | 会话、队列、随机头像缓存、限流（5.0+ 即可，无需最新版） |
| 队列 | 自写 Redis Queue Worker | 基于 Redis 列表 + BLPOP 的轻量队列，异步生成头像、审核回调，无独立队列服务 |
| 存储 | 对象存储（阿里云OSS/七牛/腾讯云COS） | 用户上传图 + 生成头像存储，返回公网URL（SDK 仅作 HTTP 调用，可按需手写签名） |
| 管理后台 | 原生 HTML + JS + CSS | 无框架无构建，可引入 Alpine.js / htmx（CDN 引入）增强交互，表格/表单纯手写 |
| 前端官网 | PHP 模板渲染（自写视图层） | 响应式，SEO 友好，PHP 直出 HTML |
| 服务端渲染（SEO） | PHP 模板 SSR | 首页与分类页 SSR 利于收录，无需 Node 环境 |
| 部署 | 宝塔面板 + Nginx + PHP-FPM | 贴合用户环境，无需 Docker，最低 1核2G 可运行 |
| AI 审核 | 可选：豆包内容审核 / 阿里云内容安全 | 头像合规判断（独立开关） |

> 说明：本方案刻意减少第三方框架依赖，仅保留 Redis 与对象存储两类基础设施型中间件。后端核心（路由、ORM、视图、队列）由项目自实现，前后端均无需 Node 构建链，部署仅需 PHP + Nginx + MySQL + Redis。

---

## 三、系统架构

### 3.1 模块总览
```
┌─────────────────────────────────────────────────────┐
│                    负载均衡 / Nginx                   │
├──────────┬──────────────┬───────────────────────────┤
│  官网前端  │  管理后台   │      对外 API 服务      │
│ (PHP/SSR) │ (原生HTML)  │ (原生PHP API)          │
├──────────┴──────────────┴───────────────────────────┤
│              业务服务层（原生 PHP + 自写 MVC）                │
│  用户  │ 积分 │ 订单 │ 头像池 │ 生成引擎 │ SEO │ 设置 │
├─────────────────────────────────────────────────────┤
│   MySQL 5.6  │  Redis  │  对象存储  │  豆包 ARK API   │
└─────────────────────────────────────────────────────┘
```

### 3.2 三层头像池设计
每个生成的头像同时归入三个池：
1. **公共头像池（public）**：所有用户共享，首页随机展示，API 可随机获取。
2. **KEY 头像池（key_pool）**：归属于某个开发者 KEY，开发者可查看其下所有子用户生成的头像。
3. **子用户头像池（sub_user_pool）**：归属于某个 KEY 下的具体子用户，方便终端用户管理自己的历史头像。

---

## 四、数据库设计（核心表）

### 4.1 用户与权限
| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `users` | 用户表 | id, username, email, password, phone, avatar, role(admin/user/developer), status, points(余额), created_at |
| `user_keys` | 开发者 KEY 表 | id, user_id, key(唯一), name, status, rate_limit, created_at |
| `sub_users` | KEY 下子用户 | id, key_id, sub_user_identifier(业务侧ID), nickname, avatar, created_at |

### 4.2 积分与订单
| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `point_products` | 积分套餐商品 | id, name, price(RMB), points, sort, status |
| `orders` | 订单表 | id, order_no, user_id, product_id, amount, points, status(pending/paid/refunded/closed), pay_method, paid_at |
| `point_logs` | 积分变动记录 | id, user_id, type(recharge/consume/refund/admin), change, balance, related_id, remark, created_at |

### 4.3 头像与生成
| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `avatars` | 头像主表（三池共用） | id, user_id(上传者), key_id(nullable), sub_user_id(nullable), origin_url(原图), result_url(生成图), prompt_id, style_id, color_id, shape_id, is_public(是否入公共池), status, created_at |
| `generation_records` | 生成记录 | id, user_id, key_id, sub_user_id, avatar_id, prompt, params(json), cost_points, api_request_id, status, error_msg, created_at |
| `prompt_templates` | 后台预设提示词 | id, title, prompt, category, is_default, status |
| `styles` | 风格配置 | id, name, prompt_suffix, icon, sort, status |
| `colors` | 颜色配置 | id, name, hex, prompt_suffix, sort, status |
| `shapes` | 形状配置 | id, name, prompt_suffix, icon, sort, status |

### 4.4 系统设置
| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `site_settings` | 站点基础设置 | key, value, group(basic/seo/points/api) |
| `seo_settings` | SEO 配置 | page, title, keywords, description, og_image |
| `audit_logs` | 操作审计日志 | id, admin_id, action, target, ip, created_at |

---

## 五、管理后台系统

### 5.1 仪表盘
- 今日生成量、注册数、充值额、积分消耗
- 头像池总量、公共池增长趋势
- 待审核订单数

### 5.2 用户管理
- 用户列表（搜索、筛选）
- 新增/编辑用户（基础信息、角色、状态）
- 手动调整积分（增加/扣减，写入 point_logs）
- 禁用/启用用户
- 查看用户 KEY 列表、生成记录、积分记录

### 5.3 开发者 KEY 管理
- 查看所有 KEY，关联用户
- 查看某 KEY 下的头像列表
- 查看某 KEY 下子用户头像历史
- 禁用/启用 KEY、重置 KEY

### 5.4 积分与商品
- 积分套餐商品 CRUD（名称、价格、积分、排序、上架）
- 积分手动增减（对指定用户）
- 积分变动记录查询（全量 + 按用户筛选）

### 5.5 订单系统
- 订单列表（按状态筛选）
- 审核订单（确认到账/驳回）
- 修改订单状态、备注
- 退款处理

### 5.6 生成与审核
- 生成记录全量查询
- AI 审核开关（全局开关 + 是否强制）
- 违规头像处理（删除、打回）
- 预设提示词 CRUD（标题、提示词、分类、是否默认）
- 风格/颜色/形状配置 CRUD

### 5.7 站点设置
- 基础设置：站名、LOGO、备案号、客服、统计代码
- SEO 设置：首页/分类页/详情页的 TDK、OG 图、sitemap
- API 设置：豆包 API Key、默认模型、单次生成扣积分数、图片尺寸
- 积分设置：注册赠送积分、生成消耗积分、最低积分阈值

### 5.8 头像池管理
- 公共头像池浏览、分类筛选、删除违规
- 按 KEY / 子用户查看头像
- 批量操作（删除、设为/取消公共）

---

## 六、对外 API 设计

### 6.1 鉴权
- Header：`X-API-Key: {developer_key}`
- 限流：按 KEY 配置 QPS，超限返回 429

### 6.2 核心接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/avatar/generate` | 上传头像图生图生成（传图URL或base64） |
| GET  | `/api/v1/avatar/random` | 从公共头像池随机获取头像（支持分类/风格筛选） |
| GET  | `/api/v1/key/avatars` | 获取当前 KEY 下的头像列表（分页） |
| GET  | `/api/v1/key/sub-users/{sid}/avatars` | 获取子用户头像历史 |
| GET  | `/api/v1/user/info` | 获取当前 KEY 关联用户信息（积分余额等） |
| GET  | `/api/v1/styles` | 获取可用风格/颜色/形状配置 |

### 6.3 生成接口参数
```json
{
  "image": "https://.../upload.jpg",
  "sub_user_id": "user_123",
  "style": "cartoon",
  "color": "pink",
  "shape": "circle",
  "prompt_override": ""
}
```
返回：
```json
{
  "code": 0,
  "data": {
    "avatar_id": 1001,
    "url": "https://.../result.png",
    "cost_points": 10,
    "remaining_points": 990
  }
}
```

---

## 七、前端官网系统

### 7.1 首页（index）
- **AI 风格视觉**：渐变背景、动态粒子/光晕、毛玻璃卡片，符合 AI 产品气质
- **测试生成区**：对接管理员预设 KEY，访客可直接上传图片测试生成（限制次数/需验证码）
- **随机头像列表**：按风格/颜色/形状分类展示公共头像池，支持分类 Tab 切换
- **SEO 优化**：SSR 渲染、TDK、结构化数据（JSON-LD）、sitemap、OG 标签
- **CTA 引导**：注册/登录/充值入口

### 7.2 用户端
- 注册（赠送积分）/ 登录 / 忘记密码
- 控制台：
  - 开发者 KEY 管理（创建、查看、重置）
  - 积分充值（套餐列表 + 支付）
  - 上传头像 → 选择风格/颜色/形状 → 生成卡通头像
  - 我的头像历史（子用户池）
  - 积分记录

### 7.3 头像生成流程
```
上传图片 → 预览 → 选择风格/颜色/形状 → 确认生成 → 扣积分 → 异步生成 → 返回结果 → 入三池
```

---

## 八、积分与支付系统

### 8.1 积分规则
- 注册赠送：X 积分（后台可配）
- 生成消耗：每次 Y 积分（后台可配，按风格可差异化）
- 充值兑换：购买套餐商品，RMB → 积分
- 退款：原路退回积分与金额

### 8.2 支付
- 接入微信支付 / 支付宝（宝塔环境常用）
- 订单状态机：pending → paid → (refunded) / closed
- 支付回调异步处理，防止掉单

### 8.3 积分流水
- 所有积分变动写入 `point_logs`，不可篡改
- 类型：注册赠送、充值、生成消耗、管理员调整、退款

---

## 九、AI 生成引擎

### 9.1 流程
1. 用户上传图片 → 存对象存储 → 得到公网 URL
2. 拼接提示词 = 预设模板 + 风格后缀 + 颜色后缀 + 形状后缀
3. 调用豆包图生图 API（异步队列）
4. 轮询/回调获取结果 URL
5. 下载结果图 → 转存自有对象存储（防止豆包链接过期）
6. 写入 `avatars` 表，同时入三池
7. 扣减积分，写入流水

### 9.2 审核开关（独立）
- 后台可配置：生成后是否自动审核
- 审核方式：调用内容安全 API 判断是否为真人头像/是否违规
- 审核不通过：标记为 `rejected`，不进公共池，可退回积分或不退（可配）

### 9.3 失败处理
- API 调用失败自动重试（最多 3 次）
- 重试仍失败：退回积分，记录错误日志

---

## 十、头像池系统

### 10.1 三池关系
```
avatars 表通过字段区分归属：
- is_public = 1        → 公共池
- key_id = xxx         → KEY 池
- sub_user_id = yyy    → 子用户池
一个头像可同时属于多个池
```

### 10.2 随机头像 API
- 从 `is_public=1` 的头像中随机抽取
- 支持按 style/color/shape 过滤
- Redis 缓存热门随机结果，降低 DB 压力

---

## 十一、SEO 系统

### 11.1 策略
- 首页、分类页、头像详情页采用 PHP 模板 SSR（自写视图层直出 HTML，无需 Node 环境）
- 每个头像生成独立详情页（`/avatar/{id}`），带 TDK 与 OG 图
- 自动生成 `sitemap.xml`（首页 + 分类 + 头像详情）
- 结构化数据：Organization、ImageObject、BreadcrumbList
- 图片懒加载 + WebP + CDN
- robots.txt 配置

### 11.2 后台 SEO 配置
- 每页可独立设置 title / keywords / description
- 默认 TDK 模板（含变量替换）

---

## 十二、安全设计

- 密码 bcrypt 哈希
- API Key 加密存储，前端不可见完整 Key
- 接口限流（按 KEY + 按 IP）
- 上传文件类型/大小校验
- SQL 注入防护（ORM 参数绑定）
- XSS 防护（输出转义）
- 操作审计日志
- 豆包 API Key 仅服务端持有，不下发前端

---

## 十三、目录结构（建议）

```
头像/
├── backend/              # 原生 PHP 后端（自写 MVC，零框架）
│   ├── app/
│   │   ├── Controllers/Api/           # 对外 API
│   │   ├── Controllers/Admin/         # 管理后台接口
│   │   ├── Controllers/Web/           # 官网页面 SSR
│   │   ├── Services/                  # 生成引擎、积分、支付
│   │   ├── Models/                    # 轻量 ORM（PDO 封装）
│   │   ├── Views/                     # PHP 模板视图
│   │   └── Jobs/                      # 队列任务（自写 Worker 消费）
│   ├── core/                          # 路由、配置、请求、响应等基础类
│   ├── database/migrations/           # SQL 迁移脚本
│   ├── public/                        # 入口 index.php + 静态资源
│   ├── routes/                        # 路由定义
│   └── config/                        # 配置文件
├── admin/                # 管理后台（原生 HTML + JS + CSS，无构建）
├── frontend/             # 官网静态资源（图片/CSS/JS，页面由 backend SSR）
├── docs/                 # 文档
└── deploy/               # 部署配置（Nginx conf、宝塔脚本，无 Docker）
```

---

## 十四、开发阶段规划

| 阶段 | 内容 | 产出 |
|------|------|------|
| P0 基础 | 数据库 + 用户/KEY/积分 + 豆包生成引擎 + 三池写入 | 可生成头像并存库 |
| P1 管理后台 | 用户管理、积分商品、订单、生成记录、预设提示词、站点设置 | 后台可运营 |
| P2 前端官网 | 首页（SSR+随机头像）、注册登录、生成流程、充值 | 用户可用 |
| P3 对外 API | 鉴权、生成/随机/列表接口、限流、文档 | 开发者可用 |
| P4 SEO 与安全 | sitemap、结构化数据、审核开关、安全加固 | 可上线 |
| P5 支付与优化 | 微信/支付宝接入、退款、性能优化、缓存 | 正式运营 |

---

## 十五、待确认事项

1. ~~后端框架：Laravel 11 还是 ThinkPHP 8？~~ → 已定：原生 PHP + 自写路由/MVC（零第三方框架）
2. ~~管理后台与前端是否分两个独立项目部署？~~ → 已定：官网由 backend PHP SSR，管理后台为独立原生 HTML 目录，均无 Node 构建
3. 对象存储选用哪家（阿里云 OSS / 七牛 / 腾讯云 COS）？
4. 支付渠道：微信支付 / 支付宝 / 都要？
5. 注册是否需要手机号验证？
6. 首页测试生成的限制策略（验证码/IP 限流/每日次数）？
7. 生成头像是否允许用户选择不入公共池？
8. 审核开关开启时，未通过审核的积分是否退还？

---

> **审核说明**：请审阅本大纲，标注需要修改/补充/删除的部分。确认后，本大纲将作为后续所有设计与开发的唯一依据。
