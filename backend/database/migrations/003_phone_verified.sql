-- ============================================================
-- 003: 用户手机验证字段
-- 配合「强制手机验证」功能：开启后未验证手机的账号无法生成头像
-- ============================================================

ALTER TABLE `users`
  ADD COLUMN `phone_verified` tinyint(1) NOT NULL DEFAULT 0 AFTER `phone`;

-- 已有手机号的老账号默认视为未验证，需用户在「个人资料」里重新绑定验证
